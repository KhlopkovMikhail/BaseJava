package com.itproger;

public class Car extends OOPTransport {

    public Car(float _speed, int _weight, String _color, byte[] _coordinate) { // Создание конструктора
        super(_speed, _weight, _color, _coordinate);
    }
}
